export default (theme) => ({
    root: {
        margin: "0 auto",
        marginTop: theme.spacing(12),
    },
    section: {
        // ...theme.flexColumnCenter,
        padding: theme.spacing(2),
        textAlign: 'center'
    }
})